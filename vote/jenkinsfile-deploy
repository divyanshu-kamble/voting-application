pipeline {
  agent { label 'Deploy'}
  stages {
    stage('kubernetes') {
      steps {
        sh '''
            pwd
            cd redis
            kubectl apply -f kubernetes
        '''
      }
    }
  }
}
