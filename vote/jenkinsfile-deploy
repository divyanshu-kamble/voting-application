pipeline {
    agent {
    label 'Deploy'
}

    // define environment variable ENVIRONMENT
    stage('kubernetes deploy') {
      steps { 
        sh '''
                kubectl apply kubernetes
        '''
      }
    }
}

